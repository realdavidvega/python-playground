lint:
	uv run ruff format . && uv run ruff check . --fix

sync:
	uv sync

chat:
	uv run python -m app.chat.main

tools:
	uv run python -m app.tools.main

memory:
	uv run python -m app.memory.main

human:
	uv run python -m app.human.main

state:
	uv run python -m app.state.main
